import 'package:flutter/material.dart';

class ColorAssets {
  static const errorColor = Colors.redAccent;
  static const primaryColor = Color.fromRGBO(50, 130, 184, 1);
  static const blackColor = Colors.black;
  static const whiteColor = Colors.white;
  static const greenColor = Colors.green;

  // shimmer colors (used in loading indicators)
  static var shimmerBaseColor = Colors.grey[300]; 
  static var shimmerHighlightColor = Colors.grey[100];
  
}